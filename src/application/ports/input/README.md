# src/application/ports/input/

Driving (input) ports — TypeScript types describing each use case's
`execute` signature. Presentation depends on these, never on use case
classes. One file per use case (`create-user-use-case.ts`).

→ Rules: [../../../../docs/architecture/ports-adapters.md#51-port-conventions](../../../../docs/architecture/ports-adapters.md#51-port-conventions)
